# Jikgwangaza

This project provides a simple interface for browsing KBO lineups and team chants. Player and chant information lives in JSON files under the `data` directory. Changes are automatically tested and deployed through GitHub Actions to Firebase Hosting.

## Development

- `npm start` – start the local development server
- `npm test` – run unit tests
- `npm run build` – create an optimized production build
